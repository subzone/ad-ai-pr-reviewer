# Functional test pipeline for ad-ai-pr-reviewer — runs on Azure DevOps.
#
# PURPOSE: Validates the task code end-to-end against this repo itself.
#          Runs the compiled Node script directly (no Marketplace install needed).
#
# REQUIRED pipeline variables (set as secret in ADO pipeline UI):
#   GITHUB_PAT         — GitHub PAT with repo + pull_requests read/write scope
#   ANTHROPIC_API_KEY  — Anthropic API key
#
# OPTIONAL pipeline variables:
#   TEST_PR_NUMBER     — An existing open PR number to run Job 1 against
#                        (leave empty to skip Job 1)

trigger: none   # manual only

pool:
  vmImage: ubuntu-latest

variables:
  TEST_REPO: subzone/ad-ai-pr-reviewer
  TEST_PR_NUMBER: ''    # set in ADO pipeline UI to test reviewPR on existing PR
  AI_MODEL: claude-sonnet-4-6

stages:
  - stage: Test
    displayName: Test AI PR Reviewer task
    jobs:

      # ── Job 1: AI review an existing PR (optional) ────────────────────────
      - job: TestReviewPR
        displayName: AI Review existing PR
        condition: ne(variables['TEST_PR_NUMBER'], '')
        steps:
          - checkout: self

          - task: NodeTool@0
            displayName: Use Node 20
            inputs:
              versionSpec: '20.x'

          - script: npm ci
            displayName: Install task dependencies
            workingDirectory: task

          - script: npm run build
            displayName: Build TypeScript
            workingDirectory: task

          - script: |
              export INPUT_ACTION="reviewPR"
              export INPUT_PROVIDER="github"
              export INPUT_ACCESSTOKEN="$(GITHUB_PAT)"
              export INPUT_REPOSITORY="$(TEST_REPO)"
              export INPUT_PRNUMBER="$(TEST_PR_NUMBER)"
              export INPUT_ENABLEAIREVIEW="true"
              export INPUT_AIAPIKEY="$(ANTHROPIC_API_KEY)"
              export INPUT_AIMODEL="$(AI_MODEL)"
              export INPUT_AIREVIEWCONTEXT="This is a test run from Azure DevOps CI."
              export INPUT_AIMAXDIFFLINES="300"
              export AGENT_TEMPDIRECTORY="$(Agent.TempDirectory)"
              export SYSTEM_DEFAULTWORKINGDIRECTORY="$(System.DefaultWorkingDirectory)"

              node task/dist/index.js
            displayName: Run task — reviewPR

      # ── Job 2: create branch → commit → createPR → AI review → cleanup ───
      - job: TestCreateAndReview
        displayName: Create PR + AI Review + Cleanup
        dependsOn: []
        variables:
          SOURCE_BRANCH: test/ado-ai-reviewer-$(Build.BuildId)
        steps:
          - checkout: self

          - task: NodeTool@0
            displayName: Use Node 20
            inputs:
              versionSpec: '20.x'

          - script: npm ci
            displayName: Install task dependencies
            workingDirectory: task

          - script: npm run build
            displayName: Build TypeScript
            workingDirectory: task

          - script: |
              set -e
              REPO="$(TEST_REPO)"
              BRANCH="$(SOURCE_BRANCH)"

              echo "=== Repo: $REPO ==="
              echo "=== Branch: $BRANCH ==="

              # Verify PAT and repo access
              echo "--- Verifying repo access ---"
              RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
                -H "Authorization: Bearer $GITHUB_PAT" \
                -H "Accept: application/vnd.github+json" \
                "https://api.github.com/repos/$REPO")
              HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
              BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS:")
              echo "Repo access status: $HTTP_STATUS"
              if [ "$HTTP_STATUS" != "200" ]; then
                echo "Cannot access repo — check PAT scopes and repo name"
                echo "Error: $BODY"
                exit 1
              fi
              echo "Repo access OK: $(echo "$BODY" | jq -r '.full_name')"

              # Get main branch SHA
              echo "--- Getting main branch SHA ---"
              RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
                -H "Authorization: Bearer $GITHUB_PAT" \
                -H "Accept: application/vnd.github+json" \
                "https://api.github.com/repos/$REPO/branches/main")
              HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
              BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS:")
              echo "Status: $HTTP_STATUS"
              if [ "$HTTP_STATUS" != "200" ]; then
                echo "Error response: $BODY"
                exit 1
              fi
              SHA=$(echo "$BODY" | jq -r '.commit.sha')
              echo "SHA: $SHA"

              # Create test branch
              echo "--- Creating branch $BRANCH ---"
              RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X POST \
                -H "Authorization: Bearer $GITHUB_PAT" \
                -H "Content-Type: application/json" \
                "https://api.github.com/repos/$REPO/git/refs" \
                -d "{\"ref\":\"refs/heads/$BRANCH\",\"sha\":\"$SHA\"}")
              HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
              BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS:")
              echo "Status: $HTTP_STATUS"
              if [ "$HTTP_STATUS" != "201" ]; then
                echo "Error response: $BODY"
                exit 1
              fi

              # Push a trivial commit so the branch has a real diff
              echo "--- Pushing test commit ---"
              CONTENT=$(printf '# ADO AI Reviewer test\nBuild: %s\n' "$(Build.BuildId)" | base64 -w0)
              RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X PUT \
                -H "Authorization: Bearer $GITHUB_PAT" \
                -H "Content-Type: application/json" \
                "https://api.github.com/repos/$REPO/contents/test-artifacts/ado-test-$(Build.BuildId).md" \
                -d "{\"message\":\"test: ADO AI reviewer run $(Build.BuildId)\",\"content\":\"$CONTENT\",\"branch\":\"$BRANCH\"}")
              HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
              BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS:")
              echo "Status: $HTTP_STATUS"
              if [ "$HTTP_STATUS" != "201" ]; then
                echo "Error response: $BODY"
                exit 1
              fi

              echo "##vso[task.setvariable variable=TEST_BRANCH]$BRANCH"
              echo "Test branch ready: $BRANCH"
            displayName: Create test branch + commit
            env:
              GITHUB_PAT: $(GITHUB_PAT)

          - script: |
              export INPUT_ACTION="createPR"
              export INPUT_PROVIDER="github"
              export INPUT_ACCESSTOKEN="$(GITHUB_PAT)"
              export INPUT_REPOSITORY="$(TEST_REPO)"
              export INPUT_SOURCEBRANCH="$(SOURCE_BRANCH)"
              export INPUT_TARGETBRANCH="main"
              export INPUT_PRTITLE="[ADO Test] AI Reviewer test run #$(Build.BuildId)"
              export INPUT_PRDESCRIPTION="Automated test PR — Build $(Build.BuildNumber). Will be closed automatically."
              export INPUT_ENABLEAIREVIEW="false"
              export INPUT_FAILONEXISTINGPR="false"
              export AGENT_TEMPDIRECTORY="$(Agent.TempDirectory)"
              export SYSTEM_DEFAULTWORKINGDIRECTORY="$(System.DefaultWorkingDirectory)"

              node task/dist/index.js
            displayName: Run task — createPR
            name: createPR

          - script: |
              export INPUT_ACTION="reviewPR"
              export INPUT_PROVIDER="github"
              export INPUT_ACCESSTOKEN="$(GITHUB_PAT)"
              export INPUT_REPOSITORY="$(TEST_REPO)"
              export INPUT_PRNUMBER="$(PrNumber)"
              export INPUT_ENABLEAIREVIEW="true"
              export INPUT_AIAPIKEY="$(ANTHROPIC_API_KEY)"
              export INPUT_AIMODEL="$(AI_MODEL)"
              export INPUT_AIREVIEWCONTEXT="Automated functional test of the AI PR Reviewer ADO extension. Briefly confirm the review pipeline is working."
              export INPUT_AIMAXDIFFLINES="100"
              export AGENT_TEMPDIRECTORY="$(Agent.TempDirectory)"
              export SYSTEM_DEFAULTWORKINGDIRECTORY="$(System.DefaultWorkingDirectory)"

              node task/dist/index.js
            displayName: Run task — reviewPR

          - script: |
              PR_URL="$(PrUrl)"
              PR_NUMBER="$(PrNumber)"
              VERDICT="$(ReviewVerdict)"
              SUMMARY="$(ReviewSummary)"
              TOTAL="$(ReviewTotalIssues)"

              case "$VERDICT" in
                lgtm)       BADGE="✅ LGTM" ;;
                needs-work) BADGE="⚠️ Needs Work" ;;
                critical)   BADGE="🚨 Critical Issues" ;;
                *)          BADGE="🔍 Reviewed" ;;
              esac

              SUMMARY_FILE="$AGENT_TEMPDIRECTORY/pr-summary.md"
              cat > "$SUMMARY_FILE" << EOF
              ## AI PR Review — $BADGE

              > $SUMMARY

              | | |
              |---|---|
              | **PR** | [#$PR_NUMBER — View on GitHub]($PR_URL) |
              | **Verdict** | $BADGE |
              | **Total Issues** | $TOTAL |
              | **Repository** | $(TEST_REPO) |
              | **Branch** | $(SOURCE_BRANCH) |
              | **Model** | $(AI_MODEL) |
              | **Build** | $(Build.BuildNumber) |
              EOF

              echo "##vso[build.uploadsummary]$SUMMARY_FILE"
            displayName: Publish summary to ADO
            condition: always()
            env:
              AGENT_TEMPDIRECTORY: $(Agent.TempDirectory)

          # Close test PR and delete branch to keep the repo clean
          - script: |
              REPO="$(TEST_REPO)"
              PR_NUMBER="$(PrNumber)"
              BRANCH="$(SOURCE_BRANCH)"

              if [ -z "$PR_NUMBER" ]; then
                echo "No PR number captured, skipping cleanup."
                exit 0
              fi

              echo "Closing PR #$PR_NUMBER..."
              curl -s -X PATCH \
                -H "Authorization: Bearer $GITHUB_PAT" \
                -H "Content-Type: application/json" \
                "https://api.github.com/repos/$REPO/pulls/$PR_NUMBER" \
                -d '{"state":"closed"}' > /dev/null

              echo "Deleting branch $BRANCH..."
              curl -s -X DELETE \
                -H "Authorization: Bearer $GITHUB_PAT" \
                "https://api.github.com/repos/$REPO/git/refs/heads/$BRANCH" > /dev/null

              echo "Cleanup done."
            displayName: Close PR + delete test branch
            condition: always()
            env:
              GITHUB_PAT: $(GITHUB_PAT)
